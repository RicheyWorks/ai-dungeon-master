//
// LivenessResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct LivenessResponse: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case up = "UP"
    }
    public enum Probe: String, Codable, CaseIterable {
        case liveness = "liveness"
    }
    public var status: Status
    public var probe: Probe

    public init(status: Status, probe: Probe) {
        self.status = status
        self.probe = probe
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
        case probe
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(status, forKey: .status)
        try container.encode(probe, forKey: .probe)
    }
}

