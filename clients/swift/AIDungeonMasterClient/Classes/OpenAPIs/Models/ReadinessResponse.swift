//
// ReadinessResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ReadinessResponse: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case up = "UP"
        case down = "DOWN"
    }
    public enum Probe: String, Codable, CaseIterable {
        case readiness = "readiness"
    }
    public var status: Status
    public var probe: Probe
    /** Present only with metrics/admin token. */
    public var sessions: Int?
    /** Present only with metrics/admin token. */
    public var engines: Int?
    /** Present only with metrics/admin token. */
    public var dependencies: [String: DependencyCheck]?

    public init(status: Status, probe: Probe, sessions: Int? = nil, engines: Int? = nil, dependencies: [String: DependencyCheck]? = nil) {
        self.status = status
        self.probe = probe
        self.sessions = sessions
        self.engines = engines
        self.dependencies = dependencies
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
        case probe
        case sessions
        case engines
        case dependencies
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(status, forKey: .status)
        try container.encode(probe, forKey: .probe)
        try container.encodeIfPresent(sessions, forKey: .sessions)
        try container.encodeIfPresent(engines, forKey: .engines)
        try container.encodeIfPresent(dependencies, forKey: .dependencies)
    }
}

